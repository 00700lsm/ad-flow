# PoC Sketch — State-Graph Agent Harness

상태: `SKETCH`  
대상: 특정 제품·직군이 아니라 **공통 에이전트 하네스** (기획 / 프론트 / 백엔드에 같은 커널, 다른 어댑터)  
이 파일: 가설·범위·성공 기준·나중에 채울 칸만 잡는다. 숫자·사례·벤더 평가는 비워 둔다.

```text
작성일:
최종 보완일:          (호스트 프로젝트 종료 후)
호스트 프로젝트:      (이 하네스를 처음 굴린 제품 저장소. 이름은 나중에)
기간:
작성자:
```

문서 성격:

```text
방법론 논문이 아니다
제품 설계서가 아니다
벤더 프롬프트 모음이 아니다
→ 하네스가 문제를 푸는지 나중에 판정하기 위한 PoC 뼈대
```

호스트 제품 문서(`REQUIREMENTS`, `ROADMAP`, 도메인 규칙)와 섞지 않는다. 제품이 끝나면 **이 파일을 채워** 일반화한다.

---

## 0. 한 줄 정의 (나중에 문장만 다듬기)

```text
초안:
직군이 달라도 에이전트를 대화 이력이 아니라
상태 전이 + 사람 게이트 + 정형 산출물(Artifact)로 묶는다.
도구(테스트, 브라우저, 스펙 린트)는 어댑터다.

확정 한 줄:
( )
```

관련 용어 (고정안, 나중에 사전만 보강):

```text
Harness        에이전트 도구·단계·정지를 강제하는 장치
State graph    단계 전이가 테스트/승인 같은 조건에만 의존
HITL           구현 전 Plan 승인 등 일상 게이트
Human Gate     기술·계약이 갈라지는 비싼 선택 게이트
Artifact       다음 단계에 넘기는 검증된 파일. 채팅 로그가 아님
Host project   하네스를 검증하기 위해 빌려 쓰는 제품 저장소
Kernel         직군 공통 루프 (Analysis → Plan HITL → 검증 → Summary)
Adapter        직군별 정본·검증기·Artifact 종류
```

---

## 1. 문제

채울 때 질문: 하네스 없이 에이전트를 쓰면 **반복해서** 무엇이 깨지는가.

```text
관찰 (스케치):
- 같은 제약이 다음 턴에 사라진다
- 범위가 Phase/에픽 단위로 팽창한다
- 추측으로 인프라·락·큐를 넣는다
- 테스트가 통과해도 제품 화면과 어긋난다
- 비용·재시도가 열린 루프로 돈다

호스트에서 실제로 일어난 일 (종료 후):
- [ ] 사례 1
- [ ] 사례 2
- [ ] 반례 (하네스가 막지 못한 일)
```

비문제 (PoC가 안 푸는 것):

```text
모델 벤치마크 1등
IDE 점유율
특정 클라우드 배포
호스트 제품의 도메인 완성도 그 자체
```

---

## 2. 가설

```text
H1  Analysis → Plan HITL → Red/Green 을 건너뛰지 않으면
    승인 없는 프로덕션 패치가 줄어든다

H2  대화 대신 Artifact만 다음 노드에 넣으면
    제약이 더 오래 유지된다

H3  한 요청 = 한 Task 이면
    에픽 통구현이 줄어든다

H4  재현·측정 없는 동시성/인프라 도입을 Human Gate에 두면
    선제 최적화 커밋이 줄어든다

H5  에이전트가 끼는 구간에만 같은 종류의 게이트를 얹으면
    기존 업무 프로세스를 바꾸지 않고도 직군별로 적용할 수 있다
    (통합 조직 프로세스 교체가 아님)

기각 조건 (나중에 숫자 넣기):
H1  ( )
H2  ( )
H3  ( )
H4  ( )
H5  에이전트 없는 일까지 강제하거나, 한 팀 게이트가 다른 팀 착수를 막으면 기각
H6  HITL이 빈 승인으로 바뀌어도 오류가 줄면 약화
```

---

## 3. 범위

### 3.1 In

```text
상태 순서 (스케치, 호스트와 이름만 맞추면 됨):

0  Governance     런타임 / 모델 티어 / 예산 / 재시도 한도
1  Analysis       읽기 전용 제약 분석
2  Plan + HITL    TDD 명세, 사람 승인 전 코드 없음
3A Red            실패하는 테스트 확인
3B Green          최소 구현. 신규 create / 기존 patch
3C Lint           동작 변경 없이
3D Refactor       테스트 유지
4  Summary        그 Task만 문서 동기화, 다음 Task로 자동 진행 금지
```

### 3.2 Out (스케치)

```text
오케스트레이터 상용 제품화
멀티 에이전트 역할 분업의 최적 토폴로지
호스트 도메인의 성능 SLA
평가 리더보드
```

### 3.3 의도적으로 느슨한 점 (종료 후 조일 곳)

```text
[ ] 샌드박스: Docker vs 로컬. 실제로 무엇이 막혔는가
[ ] 롤백: worktree 폐기 vs 수동 git restore. 한 번이라도 썼는가
[ ] 모델 스위칭: 단계별 티어가 비용에 의미가 있었는가
[ ] IDE 모드 vs 파이프라인 네이티브. 어느 쪽으로 일반화하는가
[ ] Artifact 스키마를 스키마/린터로 강제할 것인가
[ ] 공통 커널을 한 저장소에 둘지, 직군 저장소에 어댑터만 둘지
```

### 3.4 전사 프로세스에 얹을 수 있는가 (통합이 아님)

질문은 “기획·프론트·백엔드를 하나의 방법론으로 합칠 수 있는가”가 아니다.  
“이미 있는 업무 흐름 **위에**, 에이전트가 끼는 구간만, **그 업무를 막지 않고** 같은 종류의 게이트를 쓸 수 있는가”다.

```text
되는 조건:
  에이전트가 손대는 작업에만 켠다
  기존 스프린트·티켓·리뷰·QA를 대체하지 않는다
  직군 도구·완료 정의는 그대로 둔다
  한 팀의 HITL이 다른 팀 착수를 블록하지 않는다
    (기획 승인 대기가 백엔드 배포 파이프라인이 되면 지장)

안 되는 조건:
  전 직원이 같은 Artifact 폴더를 쓰게 한다
  기획 산출물이 머지될 때까지 프론트를 세운다 (이미 있는 핸드오프와 중복)
  Red를 전 직군 동일 도구로 강제한다
  에이전트 없는 수작업까지 루프에 넣는다
```

한 세트의 Jest/Gradle로 세 업무를 덮을 수는 없다.  
업무마다 **로컬로** 쓰는 것은 “멈추고, 남기고, 사람이 허가하는가” 정도다.

```text
공통 커널 (직군 불변):

  정본은 대화보다 앞선다
  한 요청 = 한 작업 단위 (에픽 통구현 금지)
  Analysis → Plan → HITL STOP → 검증 루프 → Summary STOP
  다음 단계는 채팅이 아니라 Artifact
  비싼 선택은 Human Gate
  커밋·배포·범위 확정은 사람

어댑터 (직군만 다름):

  무엇을 정본으로 읽는가
  Red에 해당하는 실패가 무엇인가
  Green의 최소 산출물이 무엇인가
  화면/스펙/API 중 무엇이 “제품 확인”인가
```

직군별 스케치 (종료 후 한 칸씩 채움. 지금 구현하지 않음):

```text
기획
  정본     문제정의, 범위, 비범위, 성공 기준, 사용자 흐름
  Analysis 모순·빠진 액터·측정 없는 목표
  Plan     이번 작업의 결정 목록과 열지 않을 결정
  Red      기준이 없거나 문서가 같은 말을 다르게 함 (실패해야 함)
  Green    합의된 스펙 조각 하나 (절, 티켓, 수락 조건)
  HITL     범위·우선순위·하지 않을 것 승인
  하지 않음  이 단계에서 화면·API 구현

프론트
  정본     수락 조건, 디자인 제약, 이미 있는 API 계약
  Analysis 라우트·상태·접근성·빈 상태
  Plan     깨질 테스트 또는 시각 회귀 목록
  Red      컴포넌트/라우트 단언 또는 명시된 화면 단언 실패
  Green    그 화면만. 새 API 발명 금지
  HITL     상호작용·빈 상태·에러 상태 범위
  제품확인  브라우저 흐름. 테스트 통과 ≠ 시연

백엔드
  정본     API/도메인 계약, 현재 구조 문서
  Analysis 영향 파일, 하지 않을 인프라
  Plan     실패하는 API/단위 테스트
  Red      테스트 러너 실패 확인
  Green    최소 패치
  HITL     계약 변경·저장소 도입은 Human Gate
  제품확인  계약 + (필요 시) 그 계약을 쓰는 화면
```

세 직군이 만나는 지점 (공통 하네스가 아니면 깨지는 곳):

```text
기획 Plan 승인 없는 프론트/백엔드 Green
프론트가 없는 API를 백엔드에 요구 → Human Gate
백엔드 테스트 통과만으로 프론트 미확인을 완료로 쓰지 않음
한 작업 단위는 한 직군 어댑터
  의존은 이전 Summary / 스펙 Artifact를 정본으로 읽음
```

PoC에서 검증할 질문:

```text
[ ] 커널 문장 10개 이내로 세 직군이 같은가
[ ] 어댑터만 바꿔 같은 HITL 문장(“승인”)이 의미가 있는가
[ ] 기획 Artifact를 프론트/백엔드 Analysis가 실제로 읽는가
[ ] 한 사람이 세 역할을 할 때 커널을 우회하는가 (기각 신호)
```

하지 말 것: 기획에 Jest를 강요하거나, 백엔드에 픽셀 회귀를 커널이라고 부르는 것.

### 3.5 사람 역할, 숙련 퇴화, 타협 (스케치)

질문이 “생산성 vs 학습 중 무엇이 맞나”로 닫히지 않게 한다.  
**생산성만 밀어 오류를 줄여도, 사람 판단이 무너지면 게이트가 먼저 실패한다.**

```text
방향 A  생산성 + 에이전트 오류 최소
  사람: 게이트 (범위, 승인, 거절)
  에이전트: 구현·탐색
  이득: 속도, 범위 팽창·선제 인프라 감소
  비용: 거절 이유를 말로 못 함. 틀린 Plan을 못 봄

방향 B  인간 학습도 키움
  사람: Red, 하지 않는 것, 가끔 Plan 원문
  에이전트: 그 안에서 Green
  이득: 이상한 패치를 보는 눈
  비용: 느림

지금 고르지 않음. 종료 후 한쪽으로 치우친 기간이 있었는지 적는다.
  실측 치우침:  A / B / 혼합 ____
```

가설 (판단 근육이 게이트의 천장):

```text
H6  HITL이 빈 승인으로 바뀌면 에이전트 오류가 다시 늘어난다
    기각: 대충 승인해도 오류가 줄거나 유지되면 약화
```

타협 (역할이 아니라 작업 종류를 나눔):

```text
반복·계약 고정     에이전트 비중 큼. 사람은 샘플만 깊게
계약·구조 분기     사람이 Plan / Human Gate. 에이전트는 초안
처음 보는 문제     사람이 재현·측정. 에이전트는 그 다음

소유 (스케치):
  사람        정본, 완료 조건, Red, 하지 않는 것, Human Gate 후보
  에이전트    Green, 린트, 기계적 반복
  둘 다       Plan 초안은 에이전트, 고쳐서 승인은 사람
```

생산성을 거의 안 깎는 학습 장치:

```text
[ ] 거절할 때 한 줄 이유를 Artifact에 남긴다
[ ] Summary / diff의 “왜”를 사람이 회수한다 (전 줄을 안 짜도 됨)
[ ] Human Gate급 Task는 사람이 Plan을 먼저 쓴다
[ ] 화면·측정은 사람이 한다. 테스트 통과 ≠ 이해
```

하지 말 것: 모든 줄을 손으로 짜라고 하는 것을 타협이라고 부르기.

생산성만 강조할 때 능력을 키우는 법 (루프 안/밖):

```text
루프 밖
  [ ] 에이전트 없이 작은 과제를 주기적으로 끝낸다. 막힌 지점 = 현재 실력
  [ ] 자기가 승인한 패치를 N일 뒤 설명한다. 못 하면 위임이 아니라 미숙
  [ ] 잘못된 승인을 에이전트 탓으로 닫지 않고, 못 본 신호로 닫는다

루프 안
  [ ] Green 수리자가 아니라 Red 설계자로 남는다
  [ ] 주당 n Task만 Plan Trade-off를 사람이 고친다  n = __
  [ ] 같은 실수 재승인을 막기 위해 거절 이유 템플릿을 쌓는다
```

종료 후 채울 증거:

```text
빈 승인 (이유 없는 승인) 횟수:     __
거절 + 한 줄 이유 횟수:           __
사람 원문 Plan 횟수:              __
일주일 뒤 설명 실패 횟수:         __
빈 승인 이후 사고/롤백:           __
```

---

## 4. 성공 기준 (빈칸)

정성만 있지 않게, 종료 후 호스트 로그로 채운다. 목표치를 지금 꾸미지 않는다.

```text
게이트
  Plan 승인 없이 머지된 구현 커밋 수:     목표 __ / 실측 __
  Human Gate 없이 들어간 인프라 커밋 수:  목표 __ / 실측 __

범위
  “에픽 전체 구현”으로 해석된 요청 중 실제 통구현 비율:  __

품질
  가짜 Green (Red가 실패하지 않는데 구현) 횟수:          __
  Summary 후 다음 Task를 같은 요청에서 이어서 한 횟수:  __
  이유 없는 승인 횟수:                                  __
  거절에 한 줄 이유가 붙은 비율:                        __

비용
  Task당 토큰 / USD 중앙값:  __
  max_retry_limit 초과 후 STOP 횟수:  __

제품 측 (하네스 성공과 분리해서 적을 것)
  호스트 데모 가능 여부:  예 / 아니오
  그건 하네스 성공이 아님. 아래 5절에 분리
```

---

## 5. 실험 설계 (스케치)

한 호스트 제품에서 하네스를 굴리는 것을 **n=1 현장 실험**으로 본다. 일반화 주장은 종료 후 한계에 적는다.

```text
독립 변수 (대략):
  HITL 유무, Artifact 유무, Task 단위 강제 유무

종속 변수:
  범위 팽창, 선제 인프라, 재작업, 사람 대기 시간

통제하지 못한 것 (나중에):
  모델 세대, 사람 숙련, 도메인 난이도, IDE 기능 변화

비교 조건 (선택, 종료 후 가능하면):
  A  자유 대화 코딩
  B  이 하네스
  동일 과제를 두 번 하는 비용이 크면 A는 회고로만
```

재현 단위:

```text
사람 요청 한 줄
→ Artifact 폴더 하나
→ 테스트 실행 기록
→ (선택) 커밋 해시
```

---

## 6. 최소 장치 (구현이 아니라 목록)

종료 후 “실제로 있었는지 / 허상이었는지”만 체크한다.

```text
[ ] 정본 문서 계층 (요구 / 구조 / 순서 / 현재 Task)
[ ] 항상 적용 규칙 + Task 스킬
[ ] config: 예산, 재시도, 모델 정책
[ ] artifacts/<id>/{analysis,plan,summary,…}
[ ] Plan HITL STOP 문장
[ ] Human Gate 보고 형식
[ ] 테스트가 전이 조건인 Red/Green
[ ] 커밋은 사람 요청 시에만
```

벤더·에디터 이름은 여기에 적지 않는다. 이식 노트는 13절.

---

## 7. 호스트에서 돌린 기록 (종료 후 채움)

제품 자랑을 쓰지 않는다. 하네스가 **막거나 실패한** 장면만.

```text
기간:
Task 수:
승인 거절 횟수와 이유:
Human Gate 횟수 / 고른 후보:
max_retry 초과:
문서-코드 충돌을 사람에게 돌려보낸 횟수:

장면 로그 (복붙 템플릿):

### 장면
- 요청 원문:
- 기대한 정지점:
- 실제 정지점:
- Artifact 경로:
- 사람이 한 말:
- 교훈:
```

---

## 8. 결과 (지금 비움)

```text
가설별:
H1  지지 / 약화 / 기각    근거:
H2  지지 / 약화 / 기각    근거:
H3  지지 / 약화 / 기각    근거:
H4  지지 / 약화 / 기각    근거:
H5  지지 / 약화 / 기각    근거:
H6  지지 / 약화 / 기각    근거:

한 문단 결론:
( )
```

하지 말 것: 호스트 데모 스크린샷만으로 하네스가 증명됐다고 쓰기.

---

## 9. 한계 (미리 적어 두는 칸)

스케치 시점의 예상. 종료 후 `실측` 칸만 추가.

```text
n=1 호스트
  예상: 도메인(광고, 백엔드, 웹)에 과적합될 수 있다
  실측:

사람 의존
  예상: 승인 품질이 사람마다 다르다. 대충 승인하면 하네스가 빈 껍데기다
  실측:

도구 허상
  예상: IDE가 규칙을 항상 읽지 않으면 루프가 문서만의 규약이다
  실측:

측정 공백
  예상: 토큰·대기 시간을 안 남기면 비용 가설을 못 판정한다
  실측:

가짜 성실
  예상: Artifact는 채우지만 Plan과 다른 코드를 쓸 수 있다
  실측:

숙련 퇴화
  예상: 생산성만 최적화하면 빈 승인이 늘고, 그 다음 에이전트 오류가 다시 는다
  실측:
```

---

## 10. 호스트 종료 후 보완 체크리스트

이 절이 이 파일의 사용법이다. 제품 회고와 별도로, **하네스 PoC를 논문/블로그/다음 저장소에 쓸 수 있는 상태**로 만드는 목록.

### 10.1 증거 수집

```text
[ ] Task ID 목록과 날짜
[ ] 거절된 Plan / 고친 Plan 쌍
[ ] Human Gate 원문과 ADR
[ ] 선제 구현을 막은 대화 vs 막지 못한 대화
[ ] 테스트 로그에서 Red 실패가 확인된 Task 비율
[ ] 커밋 메시지와 Task ID 정합
```

### 10.2 숫자

```text
[ ] Task당 턴 수, 대기 시간(사람 HITL)
[ ] 토큰 / 비용 (config 한도와 비교)
[ ] 재시도, 롤백 여부
[ ] Summary 이후 같은 세션에서 다음 Task를 시작한 비율
```

### 10.3 일반화 문장

```text
[ ] 호스트 도메인 단어를 뺀 루프 설명 한 장
[ ] 다른 언어/스택에 옮길 때 바뀌는 것 / 안 바뀌는 것
[ ] IDE 규칙 vs CI에서 같은 게이트를 돌릴지
[ ] Artifact 템플릿을 언어 중립으로 다듬기
[ ] 커널 vs 기획/프론트/백엔드 어댑터 표가 현장과 맞는지
[ ] 직군이 다른 저장소여도 Task ID·HITL 문장이 통하는지
```

### 10.4 버릴 것 / 남길 것

```text
남길 후보:
  [ ]
버릴 후보 (허식, 한 번도 안 쓴 단계, 과한 용어):
  [ ]
이름 정리 (방법론으로 오해되는 말 교체):
  [ ]
```

### 10.5 다음 PoC

```text
[ ] 두 번째 호스트 (다른 도메인)
[ ] 게이트를 CI에 옮긴 변형
[ ] Artifact 스키마 검증
[ ] 멀티 에이전트는 이 PoC 밖. 별도 가설로 분리
```

### 10.6 사람 숙련

```text
[ ] 빈 승인 vs 이유 있는 거절 비율
[ ] 사람 원문 Plan이 있던 Task 목록
[ ] 에이전트 없이 끝낸 작은 과제 기록 (있으면)
[ ] 승인 패치를 나중에 설명하지 못한 사례
[ ] 타협 규칙(작업 종류 분기)을 지켰는지 / 어겼는지
```

---

## 11. 리스크와 윤리 (스케치)

```text
사람 게이트를 우회하는 프롬프트
승인 문장을 에이전트가 위조하는 경우
측정 없이 “개선됐다”고 쓰는 것
호스트 제품 성과를 하네스 성과로 이중 계산하는 것
빈 승인으로 사람 책임을 에이전트에 전가하는 것
학습을 이유로 모든 구현을 수작업에 묶는 것 (타협이 아님)
```

대응은 종료 후 한 줄씩:

```text
( )
```

---

## 12. 의사결정 로그 (빈 표)

하네스 자체에 대한 결정만. 호스트 도메인 ADR과 분리.

```text
날짜    결정                    이유              폐기 조건
____    Task 하나 규칙          ____              ____
____    Plan 전 코드 금지       ____              ____
____    Artifact 경로 이름      ____              ____
____    (추가)
```

---

## 13. 이식 노트 (종료 후)

```text
에디터 / 런타임:
규칙 파일이 실제로 로드됐는가:
다른 저장소에 최소로 복사할 파일 목록:
  - [ ]
하지 말아야 할 복사 (호스트 도메인 규칙):
  - [ ]
```

---

## 14. 열린 질문

지금 답하지 않는다. 종료 후 지우거나 가설로 승격.

```text
[ ] HITL이 너무 많아 사람이 대충 승인하게 되지 않는가
[ ] Analysis가 의례적 문서가 되지 않는가
[ ] Red/Green이 제품 확인을 대체한다고 착각하지 않는가
[ ] Human Gate와 Plan HITL을 현업이 구분하는가
[ ] config 예산이 실제 과금을 끊는가, 문서인가
[ ] “바이브”라는 말이 루프를 가리지 않는가
[ ] 기획에 Red가 없으면 HITL만 남고 커널이 붕괴하는가
[ ] 프론트·백엔드를 한 Task로 묶으면 에픽 통구현이 돌아오는가
[ ] 빈 승인이 늘면 오류가 같이 느는가 (H6)
[ ] 생산성만 본 기간과 학습 장치를 켠 기간의 사고율이 다른가
```

---

## 15. 상태

```text
스케치     이 파일
운용 중    호스트 저장소의 규칙 / 스킬 / artifacts
완료       8·9·10절을 채운 뒤 버전을 v1로 올린다
폐기       가설이 기각되고 다음 장치에 자리를 넘긴다
```

버전:

```text
sketch.1    양식만
v1          호스트 종료 회고 반영 (아직 아님)
```
