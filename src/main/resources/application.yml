spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/adflow
    username: adflow
    password: adflow
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

server:
  port: 8080
